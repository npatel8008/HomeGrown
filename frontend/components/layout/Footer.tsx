export function Footer() {
  return (
    <footer className="mt-24 border-t border-line/80 bg-cream-deep/50">
      <div className="section flex flex-col gap-2 py-8 text-xs text-ink-faint sm:flex-row sm:items-center sm:justify-between">
        <p>HomeGrown — crop data, yields and prices are estimates, not horticultural advice.</p>
        <p>Recommendations are illustrative, not horticultural advice.</p>
      </div>
    </footer>
  );
}
